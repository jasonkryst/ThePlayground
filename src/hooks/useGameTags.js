/**
 * Hook to fetch game tags (currently not in use)
 * Will be fully implemented in Task 10
 * Returns empty tag data for now as a stub
 */
export default function useGameTags(manifests) {
  return {
    tagMap: new Map(),
    allTags: [],
  }
}
